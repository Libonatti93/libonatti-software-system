matheuslibonatti.tech, www.matheuslibonatti.tech {
	encode zstd gzip
	reverse_proxy host.docker.internal:8080

	header {
		Strict-Transport-Security "max-age=31536000; includeSubDomains"
		X-Content-Type-Options "nosniff"
		Referrer-Policy "strict-origin-when-cross-origin"
		X-Frame-Options "SAMEORIGIN"
		Permissions-Policy "camera=(), microphone=(), geolocation=(self)"
	}
}

etranslink.digital, www.etranslink.digital {
	encode zstd gzip
	reverse_proxy host.docker.internal:8081

	header {
		Strict-Transport-Security "max-age=31536000; includeSubDomains"
		X-Content-Type-Options "nosniff"
		Referrer-Policy "strict-origin-when-cross-origin"
		X-Frame-Options "SAMEORIGIN"
		Permissions-Policy "camera=(), microphone=(), geolocation=(self)"
	}
}

painel.etranslink.digital {
	encode zstd gzip
	reverse_proxy etranslink-lios-v2:8090

	header {
		Strict-Transport-Security "max-age=31536000; includeSubDomains"
		X-Content-Type-Options "nosniff"
		Referrer-Policy "no-referrer"
		X-Frame-Options "DENY"
		Permissions-Policy "camera=(), microphone=(), geolocation=(), payment=(), usb=()"
		Cache-Control "no-store"
		X-Robots-Tag "noindex, nofollow, noarchive, nosnippet"
	}
}

painel.matheuslibonatti.tech {
	encode zstd gzip
	reverse_proxy matheus-libonatti-lios-v2:8091

	header {
		Strict-Transport-Security "max-age=31536000; includeSubDomains"
		X-Content-Type-Options "nosniff"
		Referrer-Policy "no-referrer"
		X-Frame-Options "DENY"
		Permissions-Policy "camera=(), microphone=(), geolocation=(), payment=(), usb=()"
		Cache-Control "no-store"
		X-Robots-Tag "noindex, nofollow, noarchive, nosnippet"
	}
}
